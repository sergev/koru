[package]
name = "koru-macros"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "the #[koru::main] attribute"

# No syn, no quote: this is token surgery on one signature, and the workspace
# still needs no registry. See doc/Notes.md.
[lib]
proc-macro = true
